import { style } from "@vanilla-extract/css";

export const homepageFollowedArtistsStyles = style({
    paddingTop: '16px',
});

export const homepageFollowedArtistListStyles = style({
    paddingTop: '16px',
})