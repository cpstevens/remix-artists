import { pageContainerStyles } from "~/styles/pageContainer.css";

export default function ArtistsLayoutPage() {
    return <main className={pageContainerStyles}>
        <h1>Artists</h1>
        <p>This is the hub for all things artist related, showing your featured artists as well as a way to discover new ones!</p>
    </main>
}